# Scraping Dofusbook.net stuff

Quick and dirty proto to try out Scrapling. This tools will parse and serialize Dofusbook.net equipment data.

## Usage

1. Install dependencies: `pip install -r requirements.txt`
2. Run the script: `python src/run.py`
3. Check the output files: `var/inline.json` and `var/pretty.json`